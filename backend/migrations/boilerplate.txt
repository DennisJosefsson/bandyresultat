const { DataTypes } = require('sequelize')

module.exports = {
  up: async ({ context: queryInterface }) => {
    await // code
  },
  down: async ({context: queryInterface})=> {
    await // code
  }
}